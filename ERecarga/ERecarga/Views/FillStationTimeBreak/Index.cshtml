@using PagedList
@using PagedList.Mvc
@model PagedList<ERecarga.DAL.FillStationTimeBreak>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
    @using (Html.BeginForm("Index", "FillStationTimeBreak", FormMethod.Post))
    {

    <div class="form-inline">
        <label>Filtrar por Nome</label> &nbsp
        <div class="form-group">
            @Html.TextBox("procura", null, new
            {
                @class = "form-control",
                @placeholder = "Posto"
            })
        </div>
        <label>Filtrar por intervalo</label> &nbsp
        <div class="form-group">
            @Html.DropDownList("intervalo", new SelectList(ViewBag.TimeBreakList, "Value", "Text"), htmlAttributes: new { @class = "form-control" })
        </div>
        <label>Filtrar por Região</label> &nbsp
        <div class="form-group">
            @Html.DropDownList("region", new SelectList(ViewBag.ListRegions, "Value", "Text"), htmlAttributes: new { @class = "form-control" })
        </div>
        <div class="form-group">
            &nbsp <button type="submit" class="btn btn-info">Filtrar</button>
        </div>
    </div>

    }
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.First().FillStation.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().TimeBreak.Begin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().TimeBreak.End)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FillStation.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TimeBreak.Begin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TimeBreak.End)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
@Html.PagedListPager(Model, pagina => Url.Action("Index", new { pagina }))
